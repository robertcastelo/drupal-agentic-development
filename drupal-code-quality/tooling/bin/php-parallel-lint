#!/usr/bin/env bash
#ddev-generated

set -euo pipefail

# Thin shim so IDEs can invoke a tool by its usual name while we route to DDEV.
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/ddev-run" "$0" "$@"
